import React from 'react';
import ReactDOM from 'react-dom/client';
import {App} from './App.js';

ReactDOM.render( App(), document.getElementById('root')); //react API 사용
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
